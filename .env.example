# paper-search-mcp unified environment configuration
# Copy this file to .env and fill in the values you need.
# Preferred format: PAPER_SEARCH_MCP_<NAME>

# Core optional keys/tokens
PAPER_SEARCH_MCP_SEMANTIC_SCHOLAR_API_KEY=
PAPER_SEARCH_MCP_CORE_API_KEY=
PAPER_SEARCH_MCP_UNPAYWALL_EMAIL=
PAPER_SEARCH_MCP_DOAJ_API_KEY=
PAPER_SEARCH_MCP_ZENODO_ACCESS_TOKEN=
PAPER_SEARCH_MCP_GOOGLE_SCHOLAR_PROXY_URL=

# Optional provider-specific keys
PAPER_SEARCH_MCP_OPENAIRE_API_KEY=
PAPER_SEARCH_MCP_CITESEERX_API_KEY=
PAPER_SEARCH_MCP_IEEE_API_KEY=
PAPER_SEARCH_MCP_ACM_API_KEY=

# Optional: restrict which tools are registered (comma separated).
# Empty or unset registers all tools. Every registered tool is serialized into
# the tool list of every request, so exposing only what a client needs saves
# tokens in each turn. Sources are unaffected: search_papers keeps querying all
# of them regardless of which tools are exposed.
# PAPER_SEARCH_MCP_ENABLED_TOOLS=opac_suche,opac_autor_suche,opac_isbn_suche,kobv_verbund_suche,search_papers,paper_referenzen,paper_zitiert_von,zeitschrift_profil

# Optional: override env file path if needed
# PAPER_SEARCH_MCP_ENV_FILE=/absolute/path/to/.env

# Backward compatibility:
# legacy names without PAPER_SEARCH_MCP_ prefix are still supported.
